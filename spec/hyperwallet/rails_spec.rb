require 'spec_helper'

describe Hyperwallet::Rails do
  it 'has a version number' do
    expect(Hyperwallet::Rails::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
